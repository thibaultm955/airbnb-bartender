<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
  <%= link_to root_url, class: "navbar-brand" do %>
    <%= image_tag "Bartender_logo.jpg" %>
    <% end %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>


  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
        <li class="nav-item active">
          <%= link_to "Home", root_url, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Bartenders", bartenders_url, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "My Bookings", user_bookings_path(current_user.id), class: "nav-link" %>

        </li>
        <li class="nav-item dropdown">
          <%= image_tag "Profile_pic.jpg", class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
           <%= link_to "My bookings", user_bookings_path(current_user), class: "dropdown-item" %>
            <%if Bartender.where(:user_id => current_user.id) != []%>
              <%= link_to "My reservations", bartender_bookings_path(current_user), class: "dropdown-item" %>
            <%end%>
            <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          </div>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to "Login", new_user_session_path, class: "nav-link" %>
        </li>
      <% end %>
    </ul>
  </div>
</div>


<!-- <%if user_signed_in?%>
      <%if Bartender.where(:user_id => current_user.id) != []%>
        <a href="./bartenders/<%=Bartender.where(:user_id => current_user.id)[0].id%>/bookings">See all the booking for me </a>
      <%end%>
  <%end%>

-->
